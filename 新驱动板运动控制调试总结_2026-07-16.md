# 新驱动板运动控制调试总结

## 1. 文档信息

- 调试日期：2026-07-15 至 2026-07-16
- 最终分支：`新驱动板参数大调`
- 最终控制代码提交：`8677302`（本文档另行提交）
- 最终固件：已使用 Keil 5.39 编译并通过 DAP 烧录、校验
- 编译结果：`0 Error(s), 0 Warning(s)`
- 烧录结果：`Erase Done`、`Programming Done`、`Verify OK`
- 实车定位基准：SmartCarPoseMonitor 上位机摄像头位姿
- 上位机坐标单位：1 格 = 20 cm；包含外墙，与车载视觉坐标比较时去除 1 格偏移
- 无线控制链路：USB 转串口连接 HC-04，COM3，9600 baud

本文总结最终电机驱动板、四轮速度环和新版 `path_follow` 运动控制逻辑的联合调试。目标不是只让单次动作看起来正确，而是在不同方向、距离、速度、地图区域和电池状态下，找到能够兼顾速度、终点精度、航向稳定性和重复性的参数。

## 2. 最终结果概览

本轮最终完成了以下工作：

1. 为最终电机驱动板重新标定四轮速度环 PID 和八个正反向死区前馈值。
2. 修复增量式 PID 内部输出可能继续累积、超过执行器范围的问题。
3. 修复蓝牙点目标被拆成 X/Y 两段，导致相对运动走成折线的问题。
4. 优化近终点位置环的动态接管、浮点分辨率和制动速度包络。
5. 修复接近制动状态提前停住后无法恢复参考速度的问题。
6. 将平移航向保持 PID 与原地旋转 PID 分离，避免两种工况相互妥协。
7. 保留低速 yaw 前馈；后续为缩短路径耗时，删除终点航向稳定等待状态机。
8. 校准横移基础尺度、左右方向里程差和横移—纵向串轴前馈。
9. 提高 S 曲线加速度和加加速度，显著缩短中长距离动作时间。
10. 在新 S 曲线下重新增强平移航向保持 PID。
11. 通过 A/B 实测发现法向纠偏在最终强 yaw 闭环下没有收益，因此保留算法但默认关闭。
12. 使用高速闭合矩形完成综合验证，最终平面闭合误差约 0.33 cm，累计监视器 yaw 漂移约 −0.36°。

## 3. 最终参数

### 3.1 四轮速度环 PID

四个电机使用独立参数，因为最终驱动板、电机、减速箱和轮组的响应并不完全一致。

| 轮位 | Kp | Ki | Kd | 微分滤波 alpha | 输出上限 | 积分上限 |
|---|---:|---:|---:|---:|---:|---:|
| 左上 UL | 10.0 | 0.8 | 25.0 | 0.9 | 6000 | 4000 |
| 右上 UR | 14.0 | 1.1 | 20.0 | 0.9 | 6000 | 4000 |
| 左下 DL | 8.0 | 1.1 | 20.0 | 0.8 | 6000 | 4000 |
| 右下 DR | 15.0 | 1.4 | 25.0 | 0.9 | 6000 | 4000 |

对应文件：`project/code/PID_config.c`。

与旧驱动板参数相比，最终参数整体降低了过强的比例和微分作用，并针对四轮分别保留足够积分以消除稳态差。继续使用旧板的高增益会放大新驱动板的响应差异，造成速度过冲、噪声放大和四轮不同步。

### 3.2 电机死区补偿

| 轮位 | 正转前馈 PWM | 反转前馈 PWM |
|---|---:|---:|
| 左上 UL | 420 | 390 |
| 右上 UR | 495 | 429 |
| 左下 DL | 435 | 390 |
| 右下 DR | 550 | 637 |

- 补偿启用阈值：目标编码器速度绝对值不小于 2 counts。
- 补偿方式：根据目标方向，在速度 PID 输出上叠加相应正向或反向前馈。
- 目的：克服低速静摩擦和驱动板死区，避免末段目标速度不为零但车轮不动。

对应文件：`project/code/Motor.h`、`project/code/Motor.c`。

### 3.3 编码器和底盘基础参数

| 参数 | 最终值 | 来源 |
|---|---:|---|
| 单轮每圈编码器计数 | 2390 | 四轮各转 10 圈约 23900 counts |
| 有效轮径 | 0.06239 m | 地面滚动 10 圈约 1.96 m |
| 横移基础修正系数 | 0.910800 | 多距离、多区域横移实测 |
| 左移里程方向修正 | 1.0090 | 三个地图区域、五组往返平均 |
| D_X | 0.176 m | 底盘 Y 轴方向两轮中心间距 |
| D_Y | 0.20 m | 底盘 X 轴方向两轮中心间距 |
| 控制频率 | 100 Hz | 10 ms 控制周期 |

对应文件：`project/code/Motor.h`、`project/code/path_follow.c`。

### 3.4 近终点位置环

| 参数 | 最终值 |
|---|---:|
| Kp | 0.20 |
| Ki | 0.0 |
| Kd | 0.0 |
| 位置容差 | 0.015 m |
| 固定接管距离上限 | 0.22 m |
| 动态接管额外余量 | 0.12 m |
| 制动速度包络系数 | 0.35 |
| 到点最大允许实测速度 | 3.0 cm/s |

位置环不是全程控制器，而是在终点附近与 S 曲线平滑混合，用来修正制动和负载导致的残差。接管距离不再是单一常数，而是根据当前速度档和最大减速度估算制动距离，再增加 0.12 m 的滑移、执行和低速接近余量，并限制在 0.22 m 以内。

位置 PID 的输出改为直接读取控制器内部浮点输出，避免旧接口的整数返回值把小于 1 cm/s 的末段修正截断为零。

### 3.5 yaw 控制

平移保持和原地旋转采用两套独立 PID。

| 工况 | Kp | Ki | Kd | 最小低速前馈 | 角度容差 |
|---|---:|---:|---:|---:|---:|
| 平移航向保持 | 7.0 | 0.020 | 16.0 | 8.0 deg/s | 0.30° |
| 原地旋转 | 6.0 | 0.0 | 8.5 | 8.0 deg/s | 0.30° |

当前路径结束规则：

- 低速前馈死区为 0.25°；极小误差由浮点 PID 完成，较大但输出不足的误差使用 8 deg/s 前馈克服机械死区。
- 平移过程中 yaw 闭环始终有效。
- 路径满足位置到点/过目标平面且实测平移速度不大于 3 cm/s 后立即完成，不再额外冻结平移并等待最终 yaw 稳定。
- 原地旋转命令仍按 0.30°角度容差独立完成，不受本次删除影响。

两套 PID 分离的原因是：原地旋转需要更快、允许更强的角速度响应；高速平移更重视抗横移扰动力矩和阻尼。如果共用一套参数，要么旋转不够快，要么平移时容易摆动。

### 3.6 横移—纵向串轴补偿

| 方向 | 最终前馈系数 |
|---|---:|
| 左移（+Y） | +0.028 |
| 右移（−Y） | −0.035 |

补偿量为 `X 补偿速度 = 系数 × |Y 命令速度|`，左右方向独立。编码器里程计会扣除这部分人为加入的 X 前馈，因此它不会被误当成车体真实纵向位移，也不会直接与路径法向误差混淆。

本轮曾测试更大的候选值 `+0.034/−0.043`。它在地图右半区可把 80 cm 横移的纵向残差降到约 −0.10 cm 和 −0.01 cm，但在左半区却变成约 +2.22 cm 和 −1.61 cm。该相反趋势说明上位机摄像头存在局部几何畸变，不能把单一区域的最优值写进车辆。因此最终保留先前多区域标定的 `+0.028/−0.035`。

### 3.7 S 曲线速度档

| 规划距离上限 | 最大速度 | 最大加速度 | 最大加加速度 |
|---|---:|---:|---:|
| 0.30 m | 0.40 m/s | 1.70 m/s² | 8.00 m/s³ |
| 0.50 m | 0.50 m/s | 1.70 m/s² | 8.00 m/s³ |
| 0.70 m | 0.65 m/s | 1.70 m/s² | 8.00 m/s³ |
| 0.90 m | 0.75 m/s | 1.70 m/s² | 8.00 m/s³ |
| 更长距离 | 1.50 m/s | 1.70 m/s² | 8.00 m/s³ |

最大加速度由 1.05 m/s² 提高到 1.70 m/s²，最大加加速度由 3.50 m/s³ 提高到 8.00 m/s³。没有采用更激进的 2.0/10.0，是因为它虽然略快，但 100 cm 动态峰值超调达到约 1.7～2.1 cm，制动末段余量明显变小。

### 3.8 法向纠偏

算法保留，但最终默认参数为：

| 参数 | 最终值 |
|---|---:|
| Kp | 0.0 |
| 最小纠偏速度 | 0.0 cm/s |
| 最大纠偏速度 | 12.0 cm/s |
| 法向误差死区 | 0.0025 m |

新航向 PID 生效后，同一 80 cm 高速横移区段的 A/B 对比为：

| 法向纠偏 Kp | 纵向残差平均绝对值 |
|---:|---:|
| 0 | 约 0.56 cm |
| 1 | 约 1.07 cm |
| 2 | 约 1.17 cm |

编码器无法直接观测麦轮与地面之间的真实侧滑；强 yaw 闭环已控制住主要方向误差后，再加入编码器法向纠偏没有稳定收益，反而可能放大误差。因此默认关闭是基于实测的主动选择，不是删除功能。

## 4. 发现的问题、原因和修复

### 4.1 新驱动板使旧速度环参数失效

新驱动板改变了 PWM 到电机电压、电流响应和低速死区。继续使用旧 PID 时，部分轮比例和微分过强，四轮响应不一致；同一死区值也无法同时适配正反转。

修复：四轮独立 PID、八个方向死区分别标定，并给增量式 PID 内部累计输出增加 ±6000 限幅，避免执行器已饱和而内部状态继续累积。

### 4.2 蓝牙点目标被人为拆成折线路径

旧的点目标生成函数根据 X/Y 差值选择先走一个轴再走另一个轴。即使上位机下发的是一个二维目标，车也会停顿并转为第二段，表现为相对运动走成折线，并放大坐标与 yaw 的微小不一致。

修复：点目标始终生成“当前点—目标点”一条世界坐标直线段，不再插入中间拐点。

### 4.3 位置环末段量化和固定接管不合理

旧位置 PID 函数虽然内部是浮点计算，但调用端使用整数返回值，小于 1 cm/s 的修正全部丢失。固定接管距离也不能同时适配短距离低速和长距离高速。

修复：读取 PID 内部浮点输出；根据速度档、减速度和规划距离动态计算接管距离；Kp 从 0.10 提高到 0.20，制动包络系数从 0.55 降到 0.35。

### 4.4 接近制动可能提前停死

接近制动一旦锁存，如果进入时参考速度低于距离速度包络，旧逻辑只允许参考速度下降，不允许重新上升。车辆可能在目标前停住，而锁存状态又阻止位置环重新接管，形成永久欠程或超时。

修复：参考速度从包络上方或下方都按加速度限制渐进靠近包络。实测中，修复前后退 100 cm 曾停在约 96.37 cm 并超时；修复后同类前后动作均正常 `DONE`。

### 4.5 平移保持和旋转共用 yaw PID

两种工况的最优参数不同。共用参数使旋转速度、平移抗扰和末段稳定性互相牵制。

修复：新增独立的原地旋转 PID 对象；平移保持增加小积分抵消持续横移负载；所有亚角度输出改为使用浮点 PID，避免整数截断。

### 4.6 终点微小 yaw 误差拖慢路径

低速机械死区会让最后 0.3°～0.5°长期无法消除。如果无限等待，会让实际比赛路径耗时显著增加。

第一阶段曾加入低速 yaw 前馈、50 ms 稳定窗口和 200 ms 终点纯调姿硬上限。后续实车使用认为每条路径末端仍应进一步节省时间，因此在提交 `8677302` 中删除整套终点 yaw 等待状态机。现在位置条件满足后立即结束；代价是路径完成时不再额外保证 yaw 已进入 0.30°容差。

### 4.7 横移左右尺度不完全一致

麦轮滚子、载荷分布、正反转死区和装配误差导致左右横移的实际里程比例不同。

修复：基础横移系数由 0.901589 调整为 0.910800；左移里程计额外乘 1.0090。该方向修正来自新电池条件下三个地图区域的多组往返，80 cm 左右差的平均值最终约为 +0.05 cm。

### 4.8 测试脚本曾把摄像头排空时间算进控制耗时

电脑端脚本在收到 `DONE` 后会等待 4 秒，让摄像头管线中的延迟帧排空，但计时器没有及时停止，导致约 2.5～3 秒的动作被错误报告为约 7 秒。

修复：在收到 `DONE` 或超时退出运动循环时立即保存并停止运动计时，后续 4 秒只用于终点测量，不再计入控制耗时。内部状态遥测也证实车在约 2～3 秒内已结束运动。

### 4.9 上位机摄像头存在位置相关的尺度畸变

同样的 80 cm 横移，在地图中央到右侧区段可被测为约 81.1～81.8 cm，在中央到左侧区段约为 80.3～80.7 cm。不同区域相差约 1 cm，而且某些纵向残差会随地图位置反号。

处理原则：

1. 所有尺度参数尽量在地图多个区域、两个方向上测试。
2. 优先使用同一区段正反平均和闭合误差，而不是单次绝对距离。
3. 方向差异只有在多个位置重复出现时才写入车辆参数。
4. 不使用单一区域的“完美”参数覆盖已有多区域结果。

## 5. 关键实测依据

### 5.1 S 曲线对比

100 cm、120 cm/s 条件下：

- `amax=2.0, jerk=10`：启动确认后约 2.51～2.60 s 完成，动态峰值超调约 1.74～2.08 cm。
- `amax=1.7, jerk=8`：整体速度接近，但峰值超调约 1.26～1.31 cm，制动稳定性更好。

最终参数的跨距离验证：

| 距离 | 方向 | 监视器距离 | 启动确认后完成时间 | 动态峰值超调 |
|---:|---|---:|---:|---:|
| 40 cm | 前进 | 38.62 cm | 1.60 s | 1.09 cm |
| 40 cm | 后退 | 38.45 cm | 1.63 s | 1.35 cm |
| 100 cm | 前进 | 97.49 cm | 2.25 s | 1.31 cm |
| 100 cm | 后退 | 98.58 cm | 2.66 s | 1.26 cm |
| 160 cm | 前进 | 156.60 cm | 2.49 s | 未检测到越过目标 |
| 160 cm | 后退 | 156.52 cm | 2.96 s | 未检测到越过目标 |

160 cm 往返的两方向测量只差 0.08 cm，回到起点后的平面误差约 0.19 cm，因此约 −3.4 cm 的绝对距离差主要来自该地图区域的摄像头尺度，而不是前后里程不一致。

### 5.2 高速平移 yaw PID 对比

80 cm、75 cm/s 横移、高频内部状态采样条件下：

- `6/0.020/14`：右移内部峰值 yaw 约 1.41°，左移约 0.94°；监视器端点 yaw 平均绝对变化约 0.43°。
- `7/0.020/16`：右移约 1.31°，左移约 0.47°；监视器端点 yaw 平均绝对变化约 0.18°。
- `8/0.020/18`：制动末段出现约 +1.0° 后回到 −0.5°的反摆，左移端点变化也扩大到约 −0.93°。

因此 `7/0.020/16` 是抗扰和阻尼之间的最佳折中。

### 5.3 横移尺度与地图畸变

最终 S 曲线下，四段 80 cm 横移平均被监视器测为约 80.96 cm；右半区平均约 81.46 cm，左半区平均约 80.46 cm。旧 S 曲线在同一区段的结果与新 S 曲线只差约 0.38 cm，说明加速度提升只带来很小的终点变化，不应因此回退 S 曲线。

横移系数没有按单一区域的 +1%～2%误差继续修改，因为此前新电池、多区域标定的平均误差更小，而且闭合误差持续良好。

### 5.4 最终高速闭合矩形

固定同一个初始 yaw 锚点，依次执行：

1. 后退 80 cm；
2. 右移 60 cm；
3. 前进 80 cm；
4. 左移 60 cm。

监视器结果：

- 起点：约 `(2.1635, 1.2140) m`
- 终点：约 `(2.1660, 1.2119) m`
- X 闭合误差：约 +0.25 cm
- Y 闭合误差：约 −0.21 cm
- 平面闭合误差：约 0.33 cm
- 四段累计监视器 yaw 漂移：约 −0.36°
- 四段全部正常完成，无超时、无折线式目标累积

这个结果比单段绝对距离更能反映控制器是否存在系统性累计误差。

## 6. 正式功能与临时调试工具的边界

### 6.1 已永久保留的正式功能

- 四轮独立速度 PID 和方向独立死区补偿。
- 增量式 PID 内部输出限幅。
- 点目标单直线生成。
- S 曲线距离分档、动态位置环接管和接近制动恢复。
- 浮点位置/yaw 小量输出。
- 平移保持与原地旋转双 yaw PID。
- 低速 yaw 前馈；运动过程持续保持 yaw 闭环，但不再进行终点额外调姿等待。
- 横移基础尺度、左移方向里程修正、左右独立纵向串轴前馈。
- 法向纠偏算法保留但默认关闭。

### 6.2 已从车载项目删除的临时内容

- 蓝牙运行时位置 PID、接管系数、法向纠偏、S 曲线参数滑块。
- 临时 yaw 锚点捕获命令。
- `PSTATE` 距离、参考速度、实测速度、制动状态遥测。
- 临时位置 PID setter 和为调参暴露的内部变量。

删除调参辅助代码后程序体积由带遥测版本的约 224352 bytes Code 降至 222376 bytes Code；继续删除终点 yaw 等待状态机后降至 221848 bytes Code。SmartCarPoseMonitor 外部程序、电脑端测试结果和日志没有删除，以便以后复测。

## 7. Git 提交记录

| 提交 | 内容 |
|---|---|
| `39af7ab` | 最终驱动板四轮速度环 PID、八方向死区和增量 PID 输出限幅 |
| `1c20508` | 蓝牙点目标由折线路径改为单直线段 |
| `4e0b971` | 位置环 Kp、动态接管、浮点末段输出和制动包络优化 |
| `b58b380` | 第一阶段法向纠偏 Kp 从 4 降到 2 |
| `e656d4c` | 分离平移/旋转 yaw PID，加入浮点输出、低速前馈和 200 ms 终点调姿 |
| `4ee3f4b` | 横移基础尺度、左右串轴补偿和左移里程方向校准 |
| `67de4a4` | 修复接近制动参考速度低于包络时提前停死 |
| `73a991e` | S 曲线加速度 1.05→1.70，加加速度 3.50→8.00 |
| `405fc61` | 平移保持 yaw PID 6/0.02/14→7/0.02/16 |
| `beac322` | 最终 A/B 实测后将默认法向纠偏 Kp 2→0 |
| `8677302` | 删除路径终点 yaw 稳定等待，位置和低速条件满足后立即完成 |

所有本轮正式控制改动均已推送到远程分支 `新驱动板参数大调`。

## 8. 结论与使用注意事项

1. 当前参数是“最终驱动板 + 当前四个电机/轮组 + 当前车重 + 当前轮胎状态”下的联合最优结果。再次更换驱动板、电机、轮子、减速箱或显著改变载荷后，应优先复测速度环和死区。
2. 电池电压会影响高负载加速和低速死区。本轮关键多区域复测在更换新电池后完成；比赛前应保证电压状态一致。
3. 上位机监视器适合做动态趋势、方向差异和闭合误差判断，但不适合只凭单一区域的绝对厘米数做极限标定。
4. 当前法向纠偏默认关闭不是功能缺失，而是强 yaw 闭环下的实测最优选择。如果未来加入可靠外部位置反馈或换用能观测实际侧滑的传感器，可重新启用并调节。
5. 横移—纵向补偿应继续保持左右独立；不要恢复成单一对称系数。
6. 比赛真实路径应优先使用固定世界目标和同一初始 yaw 基准，避免每个相对动作都用带误差的当前角度重新定义坐标轴，从而形成累计折线。
7. 最终固件已经清除车载调参辅助代码并完成烧录校验。后续若需要复测，可继续使用外部 SmartCarPoseMonitor 和电脑端记录，但应在独立调试分支临时恢复遥测接口，避免污染比赛版本。
