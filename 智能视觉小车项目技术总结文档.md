# 智能视觉小车项目技术总结文档

本文档基于当前项目源码、头文件、MDK 工程配置、项目内说明文档，以及左右摄像头 OpenMV/Machine Vision 脚本整理。规则要求参考用户提供的 CSDN 帖子：AI 视觉小车需要通过车载摄像头观察屏幕中的推箱子游戏，在实际场地中运动，同时完成地图识别、图案/数字识别、路径规划、推箱、炸弹破墙和返回等任务。

## 一、项目整体概述

1. 这个智能小车程序实现的功能

该项目是一个基于 NXP i.MX RT1064 的智能视觉小车控制系统。小车的核心任务不是普通循迹，而是通过视觉识别屏幕中的虚拟推箱子地图，解析障碍、箱子、目标点、炸弹和小车位置，然后在主控端规划推箱路径，并用麦克纳姆轮底盘执行路径。

程序主要实现以下功能：

| 功能 | 代码位置 | 说明 |
|---|---|---|
| 地图识别输入 | `摄像头文件/左/main 新结构 .py`、`project/code/data_handle.c` | 左摄像头识别 12x16 屏幕地图，裁掉外圈后发送 10x14 地图给主控 |
| 图案/数字识别输入 | `摄像头文件/右/main - 新结构.py`、`project/code/data_handle.c` | 右摄像头运行 TFLite 模型，返回 `IMG,label,score` 或 `NUM,label,score` |
| 全局地图数据管理 | `project/code/Map_Path_Data.c/.h` | 保存障碍、箱子、目标、炸弹、小车位置和规划路径 |
| 任务状态机 | `project/code/Control.c/.h` | 控制起步、定位、取图、规划、识别、执行、错误恢复 |
| 路径规划 | `project/code/Algorithm.c`、`project/code/Game_logic.c` | 2D A*、3D 推箱 A*、炸弹捷径、多箱子任务调度 |
| 路径执行 | `project/code/path.c`、`project/code/path_follow.c` | 路径压缩、坐标补偿、S 曲线速度规划、姿态闭环 |
| 电机闭环 | `project/code/Motor.c`、`project/code/PID.c` | 编码器采样、四轮速度 PID、PWM 输出 |
| 姿态估计 | `project/code/Attitude.c`、`project/code/QuaternionEKF.c` | IMU963RA + 四元数 EKF 输出 roll/pitch/yaw |
| 菜单调试 | `project/code/Mymenu.c`、`project/code/menu.c` | IPS200 屏幕菜单、启动/复位/参数开关/地图显示 |
| 预设仿真 | `project/code/Algorithm_Test.c/.h` | 预设地图、模拟 MAP/CAR 帧、路径执行验证 |

2. 小车完成的主要任务

小车从系统角度完成了这些任务：

| 任务 | 具体实现 |
|---|---|
| 定位 | 左摄像头发送 `$CAR` 位姿帧；主控在 `handle_localization_stage()` 中多帧平均后调用 `path_follow_reset_pose()` |
| 速度控制 | `pit_1_handler()` 每 10ms 调用 `encoder_get()`、`distance_speed_strategy()`、`motor_control()` |
| 姿态控制 | `pit_2_handler()` 每 2ms 调用 `Attitude_Calculate()`；`path_follow_plan_attitude()` 用 yaw PID 输出角速度 |
| 通信 | UART1 接收地图/车姿；UART8 接收右摄像头识别结果；中断只进 FIFO，主循环解析 |
| 路径规划 | `Plan_path_Identify()`、`Plan_path_Mode1()`、`Plan_path_Mode2()` 调用底层 A* 搜索 |
| 传感器采集 | 编码器、IMU、左右摄像头、按键 |
| 电机驱动 | 四路 DIR + PWM 控制麦轮底盘，编码器闭环调速 |
| 调试显示 | IPS200 显示菜单、地图、路径、状态、速度和位姿 |

3. 从上电到主循环的通俗运行流程

上电后，`project/user/src/main.c` 的 `main()` 先初始化时钟、调试串口、屏幕菜单、两个视觉串口、Flash、电机、编码器、IMU、姿态解算、PID、运动学和路径跟踪器。然后配置 3 个 PIT 周期中断：

| 定时任务 | 周期 | 函数 | 作用 |
|---|---:|---|---|
| PIT0 | 20ms | `pit_0_handler()` | 按键扫描 `key_scanner()` |
| PIT1 | 10ms | `pit_1_handler()` | 编码器采样、路径跟踪速度计算、四轮 PID 和 PWM 输出 |
| PIT2 | 2ms | `pit_2_handler()` | IMU 姿态解算 `Attitude_Calculate()` |

进入 `while(1)` 后，主循环反复执行：

```text
control_process()
Menu_Switch()
Menu_Show()
```

其中 `control_process()` 是业务主线：先解析 UART 数据，再按当前 `g_control_stage` 推进状态机。`Menu_Switch()` 和 `Menu_Show()` 负责按键菜单与屏幕显示。

4. 中断、定时器、DMA、串口、PID、滤波、状态机

| 技术点 | 是否存在 | 具体位置 |
|---|---|---|
| 中断 | 存在 | `project/user/src/isr.c` 的 `PIT_IRQHandler()`、`LPUART1_IRQHandler()`、`LPUART8_IRQHandler()` |
| 定时器 | 存在 | `main.c` 中 `pit_ms_init(PIT_CH0/1/2, ...)` |
| DMA | 应用层未直接使用 | SDK/驱动库中存在 EDMA 文件，但当前项目业务代码未直接配置 DMA |
| 串口通信 | 存在 | `data_handle.c`，UART1 地图/车姿，UART8 图案/数字 |
| PID 控制 | 存在 | `PID.c`、`PID_config.c`、`path_follow.c` |
| 滤波算法 | 存在 | 编码器低通、IMU EKF、左摄像头多帧投票/中值采样、右摄像头模型分类 |
| 状态机 | 存在 | `Control.c` 中 `control_stage_t` 和 `control_identify_exec_state_t` |
| 路径规划算法 | 存在 | `Algorithm.c` 的 2D A*、3D A*、炸弹捷径 |

## 二、项目文件结构分析

1. 主控工程结构

MDK 工程 `project/mdk/rt1064.uvprojx` 的目标芯片是 `MIMXRT1064DVL6A`，CPU 为 Cortex-M7，工程输出名为 `rt1064`。工程主要分为 `user_c`、`code`、`zf_common`、`zf_driver`、`zf_device`、SDK、FatFs、USB 等组。业务代码主要集中在 `project/user` 和 `project/code`。

| 路径 | 作用 |
|---|---|
| `project/user/src/main.c` | 系统入口，初始化外设、PID、中断和主循环 |
| `project/user/src/isr.c` | 中断入口，分发 PIT、UART、CSI、GPIO 等中断 |
| `project/code` | 项目业务逻辑、算法、控制、姿态、电机、菜单 |
| `libraries/zf_driver` | 逐飞底层驱动：UART、PWM、PIT、encoder、GPIO 等 |
| `libraries/zf_device` | 逐飞设备驱动：IMU963RA、IPS200、按键、摄像头、无线等 |
| `libraries/sdk` | NXP SDK、启动文件、芯片寄存器和板级支持 |
| `摄像头文件/左` | 左侧地图/车姿识别摄像头程序 |
| `摄像头文件/右` | 右侧图案/数字识别摄像头程序和 TFLite 模型 |

2. 主要文件作用

| 文件 | 作用 | 典型调用关系 |
|---|---|---|
| `Control.c/.h` | 顶层流程状态机 | `main()` 调 `control_process()`；内部调 `Game_logic`、`path`、`path_follow`、`data_handle` |
| `data_handle.c/.h` | 串口协议解析 | ISR 写 FIFO，主循环调 `process_blob_data()` / `process_vision_data()` |
| `Map_Path_Data.c/.h` | 地图对象全局存储 | `data_handle` 更新，`Algorithm` / `Game_logic` 读取 |
| `Algorithm.c/.h` | 单箱路径搜索 | `Game_logic` 调 `integrated_path_output()` |
| `Game_logic.c/.h` | 多箱任务调度 | `Control.c` 调 `Plan_path_Mode1/Mode2/Identify()` |
| `path.c/.h` | 将规划路径转换为执行路径 | `Control.c` 调 `path_build_exec_from_planner()` |
| `path_follow.c/.h` | 路径跟踪和速度规划 | `pit_1_handler()` 间接调用 `path_follow_update()` |
| `Motor.c/.h` | 电机、编码器、运动学 | `pit_1_handler()` 调 `encoder_get()`、`motor_control()` |
| `PID.c/.h` | 增量式/位置式 PID | 电机闭环和路径跟踪复用 |
| `PID_config.c/.h` | 电机与姿态 PID 参数 | `main()` 中 `PID_Init()` 使用 |
| `Attitude.c/.h` | IMU 姿态解算入口 | `pit_2_handler()` 周期调用 |
| `QuaternionEKF.c/.h` | 四元数扩展卡尔曼滤波 | `Attitude_Calculate()` 调 `IMU_QuaternionEKF_Update()` |
| `kalman_filter.c/.h` | 通用卡尔曼滤波器 | 被 `QuaternionEKF` 使用 |
| `matrix.c/.h` | 矩阵运算 | 被卡尔曼滤波使用 |
| `Mymenu.c/.h` | 屏幕菜单和调试显示 | `main()` 调 `Menu_Switch()`、`Menu_Show()` |
| `menu.c/.h` | 菜单树基础结构 | `Mymenu.c` 创建菜单项 |
| `Algorithm_Test.c/.h` | 预设地图和算法仿真 | `main()` 和 `Control.c` 在测试宏开启时使用 |
| `Flash.c/.h` | Flash 保存接口 | 目前大量参数保存代码被注释，功能预留 |
| `BlueSerial.c/.h` | 蓝牙/串口调试输出 | 当前主流程中多数调用被注释 |
| `Control_Old.c/.h` | 旧版控制流程 | 当前 `main.c` 使用的是 `Control.c`，旧文件可作为历史参考 |

3. 分层划分

| 层级 | 文件/模块 | 职责 |
|---|---|---|
| 硬件驱动层 | `libraries/zf_driver`、`libraries/zf_device`、SDK | UART、GPIO、PWM、PIT、encoder、IMU、屏幕、按键等底层操作 |
| 设备封装层 | `Motor.c`、`Attitude.c`、`data_handle.c` | 将电机、IMU、UART 数据封装成项目可用接口 |
| 数据模型层 | `Map_Path_Data.c` | 保存地图对象和路径对象 |
| 算法层 | `Algorithm.c`、`Game_logic.c`、`path.c` | A* 搜索、多箱调度、路径压缩与坐标映射 |
| 执行控制层 | `Control.c`、`path_follow.c`、`PID.c` | 状态机、路径跟踪、速度/姿态/电机闭环 |
| 应用调试层 | `main.c`、`Mymenu.c`、`Algorithm_Test.c` | 初始化、主循环、菜单、预设地图测试 |
| 外部视觉层 | 左/右摄像头 Python 程序 | 屏幕地图识别、车姿识别、图案/数字识别 |

## 三、整体工作流程

1. 数据从哪里来，如何处理，最后控制什么执行器

```text
左摄像头采集屏幕地图
↓
颜色采样、多帧投票、车辆 blob 定位
↓
发送 $MAP / $CAR 串口帧
↓
主控 UART1 中断写 FIFO
↓
process_blob_data() 解析地图和车姿
↓
Control 状态机读取全局 map/car 数据
↓
Game_logic + Algorithm 规划推箱路径
↓
path_build_exec_from_planner() 生成执行路径
↓
path_follow_update() 计算 vx/vy/omega
↓
Kinematics_Inverse() 转四轮目标编码器速度
↓
motor_control() 四轮 PID 输出 PWM
↓
麦克纳姆轮底盘运动
```

右摄像头在识别阶段参与 ID 配对：

```text
Control 到达识别点
↓
uart_send_vision_img_request_by_distance() 或 uart_send_vision_num_request_by_distance()
↓
右摄像头运行图案/数字 TFLite 模型
↓
发送 IMG,label,score 或 NUM,label,score
↓
process_vision_data() 解析结果
↓
identify_action_stub() 写入 boxes[i].id / targets[i].id
↓
Mode2 按 ID 进行箱子-目标配对规划
```

2. `main()` 执行流程

`main.c` 中的初始化顺序大致如下：

```text
clock_init(SYSTEM_CLOCK_600M)
debug_init()
system_delay_ms(300)
Menu_Init()
uart_blob_init()
flash_init()
motor_init()
encoder_init()
imu963ra_init()
Attitude_Init()
PID_Init(四轮 PID + yaw/camera/gyro PID)
Kinematics_Init()
path_follow_init(GRID_SIZE_M, pulse_per_meter)
pit_ms_init(PIT_CH0, 20)
pit_ms_init(PIT_CH1, 10)
pit_ms_init(PIT_CH2, 2)
interrupt_global_enable(0)
control_init()
Algorithm_Test_PresetInput_Init(...)
while(1): control_process + menu
```

3. 主循环反复执行的内容

`control_process()` 首先处理数据，再推进状态机。当前 `ALGORITHM_TEST_ENABLE` 默认为 1，且 `main()` 会调用 `Algorithm_Test_PresetInput_Init()`，因此预设输入启用时 `control_process()` 会跳过真实 UART 数据解析，直接使用预设地图与预设车姿。正式上场时需要特别确认该宏或预设输入状态。

4. 中断服务函数或定时任务

| 中断/任务 | 文件 | 作用 |
|---|---|---|
| `PIT_IRQHandler()` | `isr.c` | 根据 PIT0/1/2 标志调用对应 handler |
| `pit_0_handler()` | `main.c` | 20ms 按键扫描 |
| `pit_1_handler()` | `main.c` | 10ms 编码器采样、路径跟踪、运动学解算、四轮 PID |
| `pit_2_handler()` | `main.c` | 2ms 姿态 EKF 更新 |
| `LPUART1_IRQHandler()` | `isr.c` | 地图/车姿 UART1 接收，调用 `uart_blob_rx_interrupt_handler()` |
| `LPUART8_IRQHandler()` | `isr.c` | 右摄像头识别 UART8 接收，调用 `vision_uart_rx_interrupt_handler()` |

5. 模块配合关系

`Control.c` 是调度中心。它不直接做 A*，而是根据阶段调用 `Plan_path_Identify()`、`Plan_path_Mode1()`、`Plan_path_Mode2()`。规划完成后，`Control.c` 调 `path_build_exec_from_planner()` 将原始规划路径变成适合物理执行的路径，再交给 `path_follow_set_path()`。

`pit_1_handler()` 是控制执行中心。它不关心推箱逻辑，只读取 `path_follow` 产生的速度命令，并将其转换成四轮闭环 PWM。

## 四、核心模块详细分析

1. 电机驱动模块

电机驱动位于 `Motor.c/.h`。系统使用四个电机，对应上左、上右、下左、下右四个麦轮：

| 电机 | DIR 引脚 | PWM 通道 |
|---|---|---|
| 上左 | `MOTOR1_DIR C7` | `PWM2_MODULE0_CHA_C6` |
| 上右 | `MOTOR2_DIR C9` | `PWM2_MODULE1_CHA_C8` |
| 下左 | `MOTOR3_DIR C11` | `PWM2_MODULE2_CHA_C10` |
| 下右 | `MOTOR4_DIR D3` | `PWM2_MODULE3_CHA_D2` |

`motor_init()` 初始化 DIR GPIO 和 17kHz PWM。`motor_pwm()` 根据速度正负设置方向引脚，根据绝对值设置 PWM 占空比。`motor_control()` 分别调用 `PID_Add_Calculate()`，用四路编码器反馈控制四个轮子的 PWM。

编码器反馈存在，`encoder_init()` 使用四路 QTIMER 正交编码器，`encoder_get()` 每 10ms 读取并清零计数。四轮目标速度来自 `Kinematics_Inverse()`。

2. 姿态/角度控制模块

姿态模块位于 `Attitude.c`、`QuaternionEKF.c`、`kalman_filter.c` 和 `matrix.c`。

`Attitude_Init()` 做三件事：

| 步骤 | 说明 |
|---|---|
| 初始化 EKF | 调用 `IMU_QuaternionEKF_Init(100, 0.00001f, 100000000, 0.9996f, 0.002, 0)` |
| 陀螺仪零偏校准 | `attitude_calibrate_gyro()` 采集最多 500 个稳定样本 |
| 初始读取 IMU | 读取 `imu963ra_get_acc()` 和 `imu963ra_get_gyro()` |

`Attitude_Calculate()` 每 2ms 调用一次。它读取 IMU，扣除 `gyroscopeOffset[]`，将陀螺仪转换为 rad/s，将加速度归一化到 g 单位，并用 `cheat_define=0.008` 做小角速度死区。之后调用 `IMU_QuaternionEKF_Update()`，输出 `eulerAngle.yaw`、`eulerAngle.pitch`、`eulerAngle.roll`。

`path_follow_plan_attitude()` 使用 `pid_yaw` 对目标 yaw 与当前 yaw 的误差做位置式 PID，输出 `omega_cmd_radps`，再参与麦轮运动学解算。

3. 编码器/里程计模块

编码器读取在 `encoder_get()`。每路编码器先进入 5 点加权平均：

```text
filtered_raw = (0.5*x4 + 0.5*x3 + 2*x2 + 3*x1 + 4*x0) / 10
```

再经过 `Lowpass(X_last, X_new)`，其中新变化量按 0.7 混合。结果保存在 `up_L_all`、`up_R_all`、`down_L_all`、`down_R_all`。

里程计更新在 `path_follow.c` 的 `update_odometry()`。它将四轮编码器速度换算为车体系 `vx_body`、`vy_body`、`omega_body`，再根据 IMU yaw 转到世界坐标，积分得到 `g_ctx.pose.x_m` 和 `g_ctx.pose.y_m`。

潜在误差来源：

| 误差来源 | 说明 |
|---|---|
| 麦轮打滑 | 推箱或转弯时编码器积分不等于真实位移 |
| IMU yaw 漂移 | `QuaternionEKF.c` 中 z 轴 gyro bias 被置为 0，yaw 长时间可能漂移 |
| 视觉定位离散 | `$CAR` 位置来自屏幕图像，可能受透视、颜色、遮挡影响 |
| 坐标补偿 | `PATH_COORD_TRANSPOSE_COMPENSATE` 和 `PATH_COORD_FLIP_VERTICAL` 必须和摄像头坐标一致 |

4. PID 控制模块

PID 通用结构在 `PID.h` 的 `tagPID_T`。项目中有两类 PID：

| 类型 | 函数 | 使用场景 |
|---|---|---|
| 增量式 PID | `PID_Add_Calculate()` | 四轮速度闭环 |
| 位置式 PID | `PID_Location_Calculate()` | 世界坐标位置修正、yaw 控制、停靠修正 |

四轮 PID 参数在 `PID_config.c`：

| PID | Kp | Ki | Kd | 积分限幅 | 输出限幅 |
|---|---:|---:|---:|---:|---:|
| `ULpid` | 9.7 | 2.2 | 0 | 4000 | 6000 |
| `URpid` | 7.7 | 2.1 | 0 | 4000 | 6000 |
| `DLpid` | 7.5 | 2.0 | 0 | 4000 | 6000 |
| `DRpid` | 9.8 | 2.1 | 0 | 4000 | 6000 |

`PID_Add_Calculate()` 的输入是当前编码器速度和目标编码器速度，输出是 PWM 增量累加后的控制量。它包含积分限幅、输出增量限幅、I 项低通滤波。`PID_Location_Calculate()` 包含 D 项低通、积分限幅、输出限幅。

`path_follow.c` 中还有路径跟踪相关 PID：

| PID | 参数宏 | 作用 |
|---|---|---|
| `pid_world_x` / `pid_world_y` | `PATH_FOLLOW_PID_WORLD_*` | 世界坐标方向速度修正 |
| `pid_stay` / `pid_stay_y` | `PATH_FOLLOW_PID_STAY_*` | 接近目标点时保持/微调位置 |
| `pid_yaw` | `PATH_FOLLOW_PID_YAW_*` | 航向角闭环 |
| `pid_accel_yaw` | `PATH_FOLLOW_PID_ACCEL_YAW_*` | 预留/辅助角速度控制 |

5. 摄像头通信模块

主控有两套互相独立的串口协议。

**左摄像头：地图/车姿**

| 项 | 内容 |
|---|---|
| 摄像头脚本 | `摄像头文件/左/main 新结构 .py` |
| 串口 | OpenMV 端 `UART(12, baudrate=115200)`，主控端 `UART_1` |
| 主控请求 | `MAP`、`CAR` |
| 地图帧 | `$MAP\n` + 10 行 14 列字符 + `$END\n` |
| 车姿帧 | `$CAR\nx_raw\ny_raw\nyaw_raw\n$END\n` |
| 地图字符 | `#` 障碍、`.` 空地、`C` 小车、`B` 箱子、`T` 目标、`D` 炸弹 |

左摄像头内部先识别 12x16 含外圈地图，`adapt_grid_to_planner_10x14()` 裁掉外圈，得到主控需要的 10x14 地图。`format_map_frame()` 保证输出 10 行、每行 14 字符。`format_car_frame()` 将连续坐标乘以 `CAR_SCALE=100` 后发送。

**右摄像头：图案/数字识别**

| 项 | 内容 |
|---|---|
| 摄像头脚本 | `摄像头文件/右/main - 新结构.py` |
| 串口 | OpenMV 端 `UART(12, baudrate=115200)`，主控端 `UART_8` |
| 模型 | `img_classifier_int8_96.tflite`、`img_int8_224.tflite`、`num_classifier_int8_96.tflite`、`num_int8_224.tflite` |
| 标签 | `image.txt` 当前为 `0` 到 `9` |
| 命令 | `1I`、`2I`、`1N`、`2N` |
| 返回 | `IMG,label,score` 或 `NUM,label,score`，失败为 `IMG,-1,-1` / `NUM,-1,-1` |

主控 `data_handle.h` 对命令的语义注释是：`1` 表示两格距离，`2` 表示一格距离；右摄像头脚本变量名写成 96/224 窗口大小。两边命令字节能对上，但变量命名语义不完全一致，建议后续统一注释。

6. 路径规划模块

路径规划分三层：

| 层 | 文件 | 核心逻辑 |
|---|---|---|
| 单步搜索 | `Algorithm.c` | 2D A* 找车路径；3D A* 找推箱路径；炸弹捷径评估 |
| 多任务调度 | `Game_logic.c` | 多箱子、多目标、多炸弹的任务选择 |
| 执行路径生成 | `path.c` | 压缩规划路径，保留关键事件点，做坐标 remap |

`Algorithm.c` 的 2D A* 状态是小车所在格子，使用四方向移动；障碍、炸弹、箱子都不可通行。3D A* 状态是 `(box_row, box_col, face)`，也就是“箱子在哪、车站在箱子哪一侧准备推”。这比普通 A* 更符合推箱子问题，因为箱子每推一步前，小车必须能走到箱子背后。

炸弹规划由 `evaluate_bomb_shortcut()` 完成：把炸弹临时加入箱子数组，作为“可推对象”推到候选墙点；然后 `simulate_bomb_explosion()` 清除 3x3 范围内障碍，再继续规划真实箱子。

`Game_logic.c` 提供三个入口：

| 函数 | 模式 | 说明 |
|---|---|---|
| `Plan_path_Mode1()` | 不按 ID 配对 | 贪心选择当前代价最小的箱子目标组合 |
| `Plan_path_Mode2()` | 按 ID 配对 | 建立 pair 表，箱子必须推到相同 ID 目标 |
| `Plan_path_Identify()` | 识别阶段 | 规划访问识别点，识别箱子/目标 ID |

7. 任务调度或状态机模块

`Control.h` 定义了主状态机 `control_stage_t`：

| 阶段 | 状态值 | 作用 |
|---|---:|---|
| `CONTROL_STAGE_IDLE` | 0 | 未启动 |
| `CONTROL_STAGE_PRESTART_MOVE` | 1 | 起步平移离开发车区 |
| `CONTROL_STAGE_IDENTIFY_LOCALIZE` | 20 | 识别阶段定位 |
| `CONTROL_STAGE_IDENTIFY_WAIT_CAMERA_DATA` | 21 | 等待识别用地图 |
| `CONTROL_STAGE_IDENTIFY_PLAN_PATH` | 22 | 规划识别路径 |
| `CONTROL_STAGE_IDENTIFY_LOAD_PATH` | 23 | 下发识别路径 |
| `CONTROL_STAGE_IDENTIFY_EXECUTE_PATH` | 24 | 执行识别路径并触发识别 |
| `CONTROL_STAGE_PUSHBOX_LOCALIZE` | 30 | 推箱阶段重新定位 |
| `CONTROL_STAGE_PUSHBOX_WAIT_CAMERA_DATA` | 31 | 等待推箱地图 |
| `CONTROL_STAGE_PUSHBOX_PLAN_PATH` | 32 | 规划推箱路径 |
| `CONTROL_STAGE_PUSHBOX_LOAD_PATH` | 33 | 下发推箱路径 |
| `CONTROL_STAGE_PUSHBOX_EXECUTE_PATH` | 34 | 执行推箱路径 |
| `CONTROL_STAGE_PUSHBOX_FINISHED` | 35 | 完成并停车 |
| `CONTROL_STAGE_ERROR` | 99 | 规划/执行异常，等待新地图重试 |

识别执行内部还有 `control_identify_exec_state_t`，用于控制“段前转向、移动、到点转向、识别、推进下一个识别点”等细节。

8. 路径实现模块

规划器输出的是格点路径，物理小车不能简单逐格走，否则会慢且抖动。`path.c` 的 `path_build_exec_from_planner()` 会：

1. 合并连续重复格子，并按优先级保留 `Position.id` 事件标记。
2. 建立关键事件点和推箱区间的前缀表。
3. 用动态规划选择更短的可执行线段。
4. 对斜线捷径做安全检查，不能跳过 `IDENTIFICATION`、`BOMB_EXPLOSION`、`PUSH_StART_POINT`、`PUSH_END_POINT`。
5. 将地图坐标转换到 `path_follow` 执行坐标。

`path_follow.c` 执行时使用 S 曲线速度规划、横向线引导、接近目标点的双轴 hold trim、yaw PID 和速度限幅来降低偏差。

## 五、优化技术分析

1. 控制算法优化

| 优化 | 代码位置 | 作用 |
|---|---|---|
| 四轮独立增量式 PID | `Motor.c` + `PID.c` | 每个轮子根据编码器反馈闭环调速 |
| PID 积分限幅 | `PID_Add_Calculate()`、`PID_Location_Calculate()` | 降低积分饱和风险 |
| D 项低通 | `PID_Location_Calculate()` | 减小微分噪声 |
| 输出限幅 | `Limit_int()`、PID 内部限幅 | 限制 PWM 和速度突变 |
| yaw 闭环 | `path_follow_plan_attitude()` | 让底盘运动时保持目标航向 |
| S 曲线速度 | `path_follow_build_scurve_profile()` | 平滑加减速，减少冲击 |
| 推箱最低速度 | `PATH_FOLLOW_PUSH_MIN_CMPS` | 防止推箱段速度过低无法推动 |

2. 数据处理优化

| 优化 | 代码位置 | 说明 |
|---|---|---|
| 串口 FIFO | `data_handle.c` | 中断只收字节，主循环解析，降低中断耗时 |
| 地图多帧投票 | 左摄像头 `VOTE_FRAMES=8` | 降低单帧颜色误判 |
| 邻域中值采样 | 左摄像头 `PATCH_USE_MEDIAN=True` | 减少局部噪点影响 |
| 编码器加权平均 + 低通 | `encoder_get()`、`Lowpass()` | 平滑速度反馈 |
| IMU EKF | `QuaternionEKF.c` | 融合陀螺仪和加速度计 |
| 视觉位置多帧平均 | `handle_localization_stage()` | `CONTROL_LOCALIZE_MIN_SAMPLES=3` |

3. 运动稳定性优化

| 优化 | 说明 |
|---|---|
| 起步平移 | `handle_prestart_move()` 先按发车方向平移，离开发车区 |
| 闭环零速停车 | `pit_1_handler()` 中 `car_stop_flag` 时继续执行 `motor_control(car_stop_array)` |
| 接近目标点微调 | `path_follow_apply_hold_trim()` 在接近目标时用 stay PID 收敛 |
| 横向误差修正 | `path_follow_apply_line_guidance()` 沿线段法向补偿偏差 |
| 爆破点暂停 | `configure_bomb_pause_for_path()` 对 `BOMB_EXPLOSION` 点暂停 500ms |
| 坐标补偿 | `PATH_FOLLOW_TARGET_X/Y_RUN_COMP_K` 根据累计轴向运动补偿目标点 |

4. 程序结构优化

项目结构比较清晰：底层驱动、数据处理、算法、控制、执行、菜单基本分离。`Control.c` 负责流程，不直接操作 PWM；`Motor.c` 负责底盘，不关心地图；`Algorithm.c` 负责搜索，不关心 UART。`path.c` 独立处理规划路径到执行路径的转换，这减少了控制层复杂度。

5. 实时性优化

| 设计 | 实时性意义 |
|---|---|
| 10ms 电机控制放在 PIT1 | 保证速度闭环周期稳定 |
| 2ms 姿态更新放在 PIT2 | 保证 yaw 数据及时 |
| UART 中断只进 FIFO | 避免中断中做字符串解析 |
| 主循环做路径规划 | 避免 A* 搜索阻塞电机中断 |
| `g_path_plan_paused` | 规划/切换路径时让 PIT1 清零速度目标，避免旧路径继续输出 |

需要注意的是，`Control.c` 中存在 `system_delay_ms()`，例如定位前相机稳定 200ms、预设识别延时 500ms。真实比赛中如果这些阻塞发生在主循环，电机中断仍会运行，但状态机响应会变慢，需要确认场景允许。

## 六、关键函数逐个解释

| 函数 | 文件 | 输入/输出 | 内部步骤 | 系统作用 |
|---|---|---|---|---|
| `main()` | `project/user/src/main.c` | 无 / 不返回 | 初始化硬件、PID、定时器、中断，进入主循环 | 系统入口 |
| `pit_1_handler()` | `main.c` | 周期中断 | 读编码器、更新路径速度、四轮 PID、输出 PWM | 10ms 运动控制核心 |
| `control_process()` | `Control.c` | 无 | 解析 UART，按 `g_control_stage` 调 handler | 顶层任务状态机入口 |
| `control_plan_path()` | `Control.c` | 无 / 成功标志 | 根据阶段调用 `Plan_path_Identify/Mode1/Mode2`，再构建执行路径 | 规划阶段核心 |
| `handle_localization_stage()` | `Control.c` | 无 | 请求/读取 CAR，多帧平均，重置 `path_follow` 位姿，转回起步 yaw | 视觉定位融合 |
| `handle_identify_execute_path()` | `Control.c` | 无 | 分段移动、转向、触发识别、记录 ID | 识别阶段执行器 |
| `identify_action_stub()` | `Control.c` | 识别目标 | 发送 IMG/NUM 请求，等待结果，写入 `boxes[].id` 或 `targets[].id` | 图案/数字识别对接 |
| `process_blob_data()` | `data_handle.c` | UART FIFO | 批量读 FIFO，拼接 stream，解析 `$MAP/$CAR` | 地图/车姿输入 |
| `parse_map_payload()` | `data_handle.c` | MAP payload / bool | 校验 10x14 或转置 14x10，更新全局对象数组 | 地图对象生成 |
| `process_vision_data()` | `data_handle.c` | UART8 FIFO | 按行解析 `NUM/IMG,label,score` | 识别结果输入 |
| `Plan_path_Mode1()` | `Game_logic.c` | 全局地图 / `car_path` | 不按 ID，贪心选择可行箱子任务 | 简单推箱规划 |
| `Plan_path_Mode2()` | `Game_logic.c` | 全局地图 / `car_path` | 建 pair 表，按 ID 选择推箱/炸弹动作 | ID 配对推箱规划 |
| `Plan_path_Identify()` | `Game_logic.c` | 全局地图 / `car_path` | 规划识别点访问路线，必要时用炸弹开路 | 首轮识别规划 |
| `integrated_path_output()` | `Algorithm.c` | 地图、箱子索引、车起点 / 路径 | 比较直推和炸弹捷径，输出最优路径 | 单箱综合规划 |
| `a_star_path_plan_3d()` | `Algorithm.c` | 地图、箱子、目标 / 路径 | 搜索 `(箱子位置, 推向)` 状态，拼接小车路径 | 推箱 A* 核心 |
| `path_build_exec_from_planner()` | `path.c` | 原始路径、地图快照 / 执行路径 | 合并重复点、保留事件、DP 压缩、坐标 remap | 规划路径到物理路径 |
| `path_follow_update()` | `path_follow.c` | 当前 yaw / `vx/vy/omega` | 更新里程计、处理暂停、算目标几何、S 曲线、yaw PID | 路径跟踪核心 |
| `distance_speed_strategy()` | `path_follow.c` | 无 | 调 `path_follow_update()`，再调 `Kinematics_Inverse()` | 生成四轮速度目标 |
| `Kinematics_Inverse()` | `Motor.c` | `vx/vy/omega` / 四轮编码器目标 | 麦轮逆运动学，换算编码器计数并限幅 | 连接路径层和电机层 |
| `motor_control()` | `Motor.c` | 四轮目标速度 | 四轮增量式 PID，限幅，调用 `motor_pwm()` | 电机闭环输出 |
| `Attitude_Calculate()` | `Attitude.c` | IMU 原始值 / `eulerAngle` | 读 IMU、零偏扣除、死区、EKF、输出欧拉角 | 姿态反馈 |

## 七、程序运行逻辑图

主流程：

```text
上电启动
↓
系统时钟与调试串口初始化
↓
菜单、UART、Flash、电机、编码器、IMU 初始化
↓
PID、运动学、path_follow 初始化
↓
PIT0/PIT1/PIT2 与 UART 中断初始化
↓
control_init()
↓
进入 while(1)
↓
control_process()
↓
Menu_Switch()
↓
Menu_Show()
↓
循环执行
```

控制状态机：

```text
IDLE
↓ 启动开关
PRESTART_MOVE
↓
IDENTIFY_LOCALIZE
↓
IDENTIFY_WAIT_CAMERA_DATA
↓
IDENTIFY_PLAN_PATH
↓
IDENTIFY_LOAD_PATH
↓
IDENTIFY_EXECUTE_PATH
↓ 识别完成或切 Mode1
PUSHBOX_LOCALIZE
↓
PUSHBOX_WAIT_CAMERA_DATA
↓
PUSHBOX_PLAN_PATH
↓
PUSHBOX_LOAD_PATH
↓
PUSHBOX_EXECUTE_PATH
↓
PUSHBOX_FINISHED
```

10ms 速度控制中断：

```text
PIT1 触发
↓
encoder_get()
↓
若 control_is_path_plan_paused():
    清零 speed_three_array 和 speed_encoder
否则:
    distance_speed_strategy()
↓
根据 car_go_flag / car_stop_flag
↓
motor_control(speed_encoder 或 car_stop_array)
↓
PID 输出 PWM
```

串口通信流程：

```text
摄像头发送字节
↓
LPUARTx_IRQHandler()
↓
uart_xxx_rx_interrupt_handler()
↓
写入 FIFO
↓
主循环 process_blob_data/process_vision_data
↓
解析完整帧或完整行
↓
更新 map_data/car_pose/vision_result
```

路径规划流程：

```text
读取地图对象
↓
识别阶段：Plan_path_Identify()
或
推箱阶段：Plan_path_Mode1()/Plan_path_Mode2()
↓
单箱任务调用 integrated_path_output()
↓
直推 3D A* 与炸弹捷径比较
↓
合并多段路径到 car_path
↓
path_build_exec_from_planner()
↓
下发 path_follow_set_path()
```

路径跟踪流程：

```text
path_follow_update(yaw)
↓
update_odometry()
↓
处理暂停点/目标点到达
↓
计算当前位置到目标点的距离和方向
↓
S 曲线生成参考速度
↓
线引导/hold trim 修正 vx/vy
↓
yaw PID 生成 omega
↓
世界速度转车体速度
↓
distance_speed_strategy() 调 Kinematics_Inverse()
```

## 八、代码优点与可能存在的问题

1. 程序结构优点

| 优点 | 说明 |
|---|---|
| 分层清晰 | 视觉输入、地图数据、路径规划、路径执行、电机驱动基本解耦 |
| 状态机完整 | `Control.c` 将识别阶段和推箱阶段分开，便于调试 |
| 串口解析可靠 | FIFO + stream/line buffer，地图和识别结果分离 |
| 路径规划能力强 | 支持推箱、炸弹破墙、ID 配对、多任务调度 |
| 执行层优化丰富 | S 曲线、线引导、暂停事件、目标补偿、坐标 remap |
| 菜单调参方便 | 可开关启动、复位、斜线捷径、后续视觉定位、段前预转向 |

2. 控制方法优点

四轮速度闭环让底盘输出更稳定；IMU yaw 参与姿态闭环，能减小路径执行时车头偏转；编码器里程计与视觉定位结合，能兼顾实时性和绝对位置校正；`car_stop_flag` 使用闭环零速停车，避免直接断 PWM 后滑行。

3. 可能导致误差或不稳定的地方

| 问题 | 影响 |
|---|---|
| `ALGORITHM_TEST_ENABLE` 默认为 1 | 上场时可能仍使用预设地图，真实摄像头数据被跳过 |
| 右摄像头无明显置信度阈值 | 最高分即返回，低置信度也可能被当作有效 ID |
| `path_follow_set_external_position()` 是空实现 | 后续视觉定位不是平滑融合，只能通过 `path_follow_reset_pose()` 硬校正 |
| yaw 长时间漂移 | `QuaternionEKF.c` 中 z 轴 gyro bias 被置 0，长期 yaw 漂移可能影响路径 |
| 注释编码乱码较多 | 降低维护和答辩讲解可读性 |
| `Control.c` 体量很大 | 状态机、识别、定位、规划保护都在一个文件，后续扩展成本较高 |
| 炸弹外墙保护未显式建模 | 当前主控只处理 10x14 内部地图；如果把外墙也纳入地图，需要保护外墙不被 `simulate_bomb_explosion()` 清除 |
| 阻塞延时存在 | `system_delay_ms()` 会降低主循环响应速度，虽然电机中断仍运行 |

4. 需要现场调试的参数

| 参数 | 文件 | 作用 |
|---|---|---|
| 四轮 PID | `PID_config.c` | 影响四轮速度响应、超调、稳态误差 |
| `GRID_SIZE_M` | `Map_Path_Data.h` | 虚拟格点到实车距离映射 |
| 轮距/编码器参数 | `Motor.h` | 影响运动学和里程计 |
| `PATH_FOLLOW_SCURVE_*` | `path_follow.c` | 影响加减速和最高速度 |
| `PATH_LINE_GUIDE_KP` | `path_follow.c` | 影响横向偏差修正强度 |
| 左摄像头颜色参考值 | 左脚本 `REF_R/G/B/T` | 影响地图分类 |
| 左摄像头透视四角点 | 左脚本 `RECT_TL/TR/BR/BL` | 影响采样点位置 |
| 右摄像头模型窗口 | 右脚本 `switch_window()` | 影响识别目标裁剪 |
| 起步补偿 | `g_control_prestart_depart_compensate_m` | 影响离开发车区后的初始定位 |

## 九、改进建议

1. 控制稳定性

建议在 `path_follow.c` 中加入速度命令变化率限制，尤其是 `vx_body_cmps`、`vy_body_cmps` 和 `omega_cmd_radps` 的最终输出处。四轮 PID 当前 Kd 为 0，若现场出现速度震荡，可先调低 Ki，再适当增加阻尼项或速度前馈。

2. 定位精度

建议把 `path_follow_set_external_position()` 实现为轻量融合，而不是完全空函数。可以采用低通融合：

```text
pose = pose * (1 - alpha) + vision_pose * alpha
```

这样后续视觉定位不会像 `path_follow_reset_pose()` 那样产生突变。

3. 传感器数据可靠性

右摄像头建议增加置信度阈值和多帧确认。例如低于 60 或 70 的结果返回 `-1,-1`，连续两次一致再提交 ID。左摄像头可把 `detect_game_finished_blue()` 和 `detect_return_start_blue()` 与主控协议联动，减少主控对路径完成的单一依赖。

4. 代码可读性

建议统一修复 C 文件注释编码，尤其是 `Control.c`、`path_follow.c`、`data_handle.c`。这些文件是答辩中最常被问到的核心文件，乱码会显著降低阅读效率。

5. 模块化程度

`Control.c` 可以继续拆分为：

| 建议文件 | 内容 |
|---|---|
| `Control_Localize.c` | 定位、CAR 请求、位姿融合 |
| `Control_Identify.c` | 识别目标收集、识别请求、ID 保存 |
| `Control_Plan.c` | 规划保护、快照、路径构建 |
| `Control_State.c` | 主状态机转移 |

6. 调试便利性

建议恢复或完善 `BlueSerial_PathDebugReport()`，周期输出当前 stage、pose、target、speed、yaw error、map frame count、vision score。这样现场不必只靠 IPS200 小屏定位问题。

7. 运行效率

`Game_logic.c` 已有缓存和剪枝。若地图复杂后仍卡顿，可把 Mode2 的候选动作数量、炸弹候选墙数量做成菜单参数，或者在规划超时后退化为较保守的 Mode1/局部最短方案。

8. 流程改进

上场前建议明确两套运行模式：

| 模式 | 配置 |
|---|---|
| 仿真/调试 | `ALGORITHM_TEST_ENABLE=1`，使用预设地图 |
| 实车比赛 | 关闭预设输入，确保 `process_blob_data()` 和 `process_vision_data()` 正常处理真实摄像头 |

同时建议在启动界面显示当前是否处于预设地图模式，避免比赛时误用测试输入。

